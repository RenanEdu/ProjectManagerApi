import {  useEffect, useState } from 'react';
import { api } from '../services/api';

type Project = {
    id: number;
    name: string;
    description: string;
};

export default function ProjectList() {
    
}